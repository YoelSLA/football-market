spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/football_market_test
    username: postgres
    password: root

    hikari:
      data-source-properties:
        TimeZone: UTC

  jpa:
    properties:
      hibernate:
        jdbc:
          time_zone: UTC

jwt:
  secret: test-secret-key-for-football-market-2026
  expiration: 3600000

logging:
  level:
    org.hibernate.SQL: OFF
    org.hibernate.orm.jdbc.bind: OFF
    org.hibernate.stat: OFF